{
  "name": "career-gps-backend",
  "version": "1.0.0",
  "description": "Secure and scalable backend API for the Career GPS Assessment App",
  "main": "server.js",
  "type": "module",
  "engines": {
    "node": ">=20.x"
  },
  "scripts": {
    "start": "node --dns-result-order=ipv4first server.js",
    "dev": "nodemon --dns-result-order=ipv4first server.js"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.2.0",
    "mongoose": "^8.3.1",
    "multer": "^2.2.0",
    "nodemailer": "^6.9.13"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
